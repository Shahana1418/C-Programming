#include<stdio.h>
int main()
{
    int a,b,c;
    printf("enter the value of a,b and c:");
    scanf("%d%d%d",&a,&b,&c);
    if(a>b&a<c)
    printf("c is greater");
else if(b>c&b<a)
printf("a is greater");
else
printf("b is greater");
}